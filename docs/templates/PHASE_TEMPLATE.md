# <Milestone Or Feature> Phase <N>: <Name>

This is an optional detailed phase artifact. Light normally needs no phase
document; Standard may use a short plan/result; Heavy uses only purposeful
documents within its default 3-5 document package.

## Authority And Boundary

- Objective:
- Authority / permission:
- Allowed / read-only / forbidden paths:
- Dependencies and non-goals:
- ACTIVE_CONTEXT:

## Acceptance And Checks

- Observable acceptance:
- Affected contracts:
- Focused checks:
- Project-required CI/runtime/UI/security checks, if selected:
- Human-only gates and stop conditions:

Browser, data, redaction, and threat-model checks exist only when project
authority selects them. Graph, waves, lanes, adapters, ledgers, and packets are
conditional profiles.

## Result

- Changed surfaces and evidence refs:
- Mechanical direct-check closure, if applicable:
- Targeted semantic re-review, if applicable:
- Skipped/unavailable checks and concerns:
- ACTIVE_CONTEXT next-action update/proposal:

Apply `docs/SAGE_CORE.md#sage-completion-001`; phase completion never creates
product acceptance.
